---
dependencies:
  - role: nginx
  - role: dehydrated
    when: mailhog.use_dehydrated
  - role: oauth2_proxy
    when: (mailhog.oauth2_proxy is defined and mailhog.oauth2_proxy != None)

galaxy_info:
  author: "punkt.de"
  license: "MIT"
  role_name: mailhog
  description: "Mailhog role for Proserver"
  namespace: "punktde"
  min_ansible_version: "2.19"
  platforms: []
